// Bubba's Coffee — global JS
// Kept minimal on purpose: every extra KB here costs INP/LCP budget.
document.addEventListener('DOMContentLoaded', function () {
  // Placeholder for cart drawer / mobile nav toggle wiring as sections are built out.
});
